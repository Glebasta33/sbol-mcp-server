##### Описание
Cookbook MCP Server для разработки SBOL Android.
Предоставляет доступ к документации по СБОЛ, который GigaCode может использовать в качестве дополнительного контекста при решении задач.

##### Реализованный подход
✅ **На каждую задачу - отдельная тулза, которые работают с md файлами (MVP)**

Функции:
- ✅ Предоставление доступа к шаблонам решения задач
    - ✅ Data & Domain слои (dto, converters, repo, usecase, mappers)
    - 🔄 Unit-tests (планируется)
    - 🔄 Каркас (экран + VM) (планируется)
- ✅ Доступ к актуальной документации через промпты

##### Архитектура

## ✅ Реализовано (v1.0.0)

### Структура проекта (Clean Architecture)

```
src/main/kotlin/prototype/
├── core/                    # ✅ Общие утилиты и конфигурация
│   ├── config/              # ✅ AppConfig - централизованная конфигурация
│   └── result/              # ✅ Result<T> - типобезопасная обработка ошибок
│
├── domain/                  # ✅ Бизнес-логика (интерфейсы)
│   ├── model/               # ✅ Доменные модели (ToolContext)
│   └── service/             # ✅ Интерфейсы (FileService, ContextService)
│
├── data/                    # ✅ Реализации сервисов
│   └── service/             # ✅ FileServiceImpl, ContextServiceImpl
│
├── presentation/            # ✅ MCP Tools
│   └── tools/               # ✅ Все tools + ToolRegistry
│       ├── HelloTool.kt
│       ├── EchoTool.kt
│       ├── GetTimeTool.kt
│       ├── CalculateTool.kt
│       ├── SystemInfoTool.kt
│       ├── DataDomainContextTool.kt
│       └── ToolRegistry.kt
│
├── prompts/                 # ✅ MD файлы с правилами и шаблонами
│   └── data-domain-layer.md
│
└── Main.kt                  # ✅ Точка входа с DI
```

### Ключевые компоненты

#### Core Layer ✅
- `AppConfig` - все константы и пути
- `Result<T>` - обработка результатов (Success/Error)
- `AppError` - иерархия ошибок

#### Domain Layer ✅
- `FileService` - интерфейс работы с файлами
- `ContextService` - интерфейс работы с контекстами
- `ToolContext` - модель контекста с метаданными

#### Data Layer ✅
- `FileServiceImpl` - чтение файлов с обработкой ошибок
- `ContextServiceImpl` - обработка промптов

#### Presentation Layer ✅
- `ToolRegistry` - централизованная регистрация tools
- Отдельные файлы для каждого tool
- Extension functions для Server

### Доступные Tools ✅

1. **hello** - приветствие
2. **echo** - эхо сообщений
3. **get_time** - текущее время
4. **calculate** - арифметические операции
5. **system_info** - информация о системе
6. **get_data_domain_context** - контекст для генерации Data/Domain слоёв

### Документация ✅

- `README.md` - полное описание проекта
- `ARCHITECTURE.md` - детальная архитектура
- `QUICK_START.md` - быстрый старт
- `CHANGELOG.md` - история изменений

## 🔄 Планируется

### Новые Tools
- [ ] `get_unit_test_template` - шаблоны для unit-тестов
- [ ] `get_screen_template` - каркас экрана + ViewModel
- [ ] `get_ui_component_template` - шаблоны UI компонентов
- [ ] `list_prompts` - список доступных промптов
- [ ] `read_prompt` - чтение конкретного промпта

### Улучшения архитектуры
- [ ] Unit тесты для всех слоёв
- [ ] Koin для DI
- [ ] Логирование через SLF4J
- [ ] Кеширование файлов
- [ ] Асинхронность через корутины

### Новые промпты
- [ ] `unit-test-template.md` - правила написания тестов
- [ ] `screen-template.md` - структура экрана
- [ ] `ui-components.md` - UI компоненты
- [ ] `navigation.md` - правила навигации

---

**Статус**: ✅ MVP реализован  
**Версия**: 1.0.0  
**Обновлено**: 16.12.2025
